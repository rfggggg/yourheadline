server:
  port: 8080
# spring-data 配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yourheadline?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone = Asia/Shanghai
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update # 第一次建表用 create ，后面用 update
    show-sql: true  # 是否显示操作的 sql 语句